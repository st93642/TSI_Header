# Task: Blocks and Procs

# 1. Create a method that takes a block and applies it to a number
def apply_to_five(&block)
  # Call the block with 5
end

# 2. Create a method that returns a Proc that adds n
def make_adder(n)
  # Return a Proc that adds n to its argument
end

# 3. Create a method that uses a block to filter array
def custom_select(arr, &block)
  # Filter arr using the block
end
