#include <stdio.h>

int main(void)
{
    const char *studentName = "Alice Johnson";
    const char *programme = "Computer Science";
    const int totalCredits = 180;
    const int completedCredits = 32;

    // TODO: Print the student and programme details on separate lines using printf.

    int remainingCredits = 0;
    double completionPercentage = 0.0;

    // TODO: Compute remainingCredits and completionPercentage (cast to double first).
    // TODO: Print both values, formatting the percentage with one digit after the decimal point.

    int studyDays = 0;
    int minutesPerDay = 0;

    // TODO: Ask the user for studyDays and minutesPerDay with printf prompts and read with scanf.

    int totalMinutes = 0;
    double averagePerDay = 0.0;

    // TODO: Calculate the total minutes and the average per day (avoid dividing by zero).
    // TODO: Print the study summary exactly like the expected output.

    return 0;
}
